# Copier vers /etc/nginx/snippets/autonomous-learning-os-mutation-secret.conf,
# propriétaire root:root, permissions 0600. Utiliser exactement la même valeur
# que CURRICULUM_MUTATION_SECRET dans runtime.env.
proxy_set_header X-Learning-OS-Mutation-Secret "replace-with-the-runtime-secret";
